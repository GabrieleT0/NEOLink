'use strict';

/**
 * isced-narrow-field router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::isced-narrow-field.isced-narrow-field');
